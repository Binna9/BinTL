bind = "0.0.0.0:8083"
data_dir = "./data"
max_upload_mb = 512
max_concurrent_jobs = 1

session_secret = "8f5dfb05457f7290e365047c051fa03a9dacf7ad656853eb98567782af5842a0"
encryption_secret = "c0031e97a128edd12b8c17e64ece20b9426730e28cc5b90ccb29a930dfc6ba2d"

# 개발만. 운영은 false.(로그인 인증 미적용)
skip_auth = false

# Oracle/Tibero. [odbc]를 안 쓰면 등록된 드라이버를 자동으로 고른다.
# [odbc]
# oracle_driver = "..."   # odbcinst.ini에 나온 드라이버 이름
# tibero_driver = "..."
# sys_ini = "/etc"        # odbcinst.ini가 /etc가 아닐 때만
